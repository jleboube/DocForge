# Readwise-like Features

- Kindle clippings parser (`==========` split)
- Highlight normalization (book, author, text, location, date)
- Highlight-level dedupe using source + text + location
- Highlight collection searchable independently
- Manual highlight creation endpoint
